# Copy to .env and fill with real credentials (DO NOT COMMIT .env)
DATABASE_URL=postgres://user:password@host:5432/dbname
NODE_ENV=development

# Unisender configuration (recommended for this project)
# 1) Sign in to https://www.unisender.com/ (you already registered)
# 2) Go to SETTINGS → API → Create API key (or see API keys in your account)
# 3) Copy that API key and paste it below as UNISENDER_API_KEY
# 4) Set a verified sender email in Unisender or use the email you control

UNISENDER_API_KEY=
UNISENDER_FROM_EMAIL=your-verified@domain.com
UNISENDER_FROM_NAME="CyberWhale"

# (Optional) If you prefer SMTP or another provider as fallback, you can still set SMTP_* vars
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_pass
# SMTP_FROM="CyberWhale <no-reply@yourdomain.com>"
